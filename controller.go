package main

/*
 * Run the RAT controller.
 *
 * RAT controller is implemented as a custom, small, stateful interactive CLI.
 * I tried to find a lib to do this for me, but it doesn't look like there's a nice standard interactive CLI
 * lib that's widely used. It's not hard to write one, however.
 */
func runAsController() {
	/*fmt.Println("Running as the RAT controller...")
	fmt.Println("Going interactive!\n")

	desc := "Controller component, argument parser."
	prompt := "RAT-CONTROLLER> "*/

}
